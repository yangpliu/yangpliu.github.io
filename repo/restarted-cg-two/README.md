# Build

Build from this directory with:

```text
pdflatex paper.tex
bibtex paper
pdflatex paper.tex
pdflatex paper.tex
```

The supplied TeX source names `paper.bib` directly. `references.bib` is an
identical repository copy of that database.
