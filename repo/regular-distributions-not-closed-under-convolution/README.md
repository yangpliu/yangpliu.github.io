# Regular distributions are not closed under convolution

- `paper.pdf` is the audited, repository-ready manuscript.
- `paper.tex` is the submitted source with only the required provenance note
  inserted.
- `paper.bbl` is the submitted prebuilt bibliography. No BibTeX database was
  supplied, so retain this file when rebuilding from a clean state.

To rebuild:

```text
pdflatex paper.tex
pdflatex paper.tex
pdflatex paper.tex
```
